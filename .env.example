# Only required for `mem save --ai`.
# The Anthropic SDK reads this variable directly; never commit a real key.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: override the default Claude model used for --ai drafting.
MEM_MODEL=claude-sonnet-5
